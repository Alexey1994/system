/*
#include <stdio.h>
#include <stdlib.h>

#include "STUN.h"


int main()
{
    initialize_STUN();

    char           mapped_host[16];
    unsigned short mapped_port;

    get_STUN_MAPPED_ADDRESS("stun.ideasip.com", 3478, mapped_host, &mapped_port);

    printf("%s:%d", mapped_host, mapped_port);

    return 0;
}
*/
