#include "REALM.h"


void print_REALM_attribute(Byte *attribute, int length)
{
    printf("\tREALM, %d bytes\n", length);
}


void read_REALM_attribute(STUN_Attributes *attributes, Byte *attribute, int length)
{

}
